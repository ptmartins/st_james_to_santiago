---

---

<section class="section section--hero">
    <picture class="hero-bg">
        <source srcset="/images/hero.webp" type="image/webp" />
        <img
            src="/images/hero.webp"
            alt="Hero"
            width="..." height="..."
            fetchpriority="high"
            decoding="async"
            loading="eager"
        />
    </picture>

    <div class="inner-container">
        <h1 class="hero__title">From St. James to Santiago</h1>
        <h3 class="hero__subTitle">What happens when you turn a personal challenge into a project to also give something back?</h3>
    </div>
    <div class="hero__overlay"></div>
</section>

<style>
    .section--hero {
        margin-block: 0;
        min-height: 100vh;

        .inner-container {
            inset-block-start: 50%;
            inset-inline-start: 50%;
            position: absolute;
            translate: -50% -50%;
            width: 100%;
        }
    }

    .hero-bg {
        display: block;
        filter: brightness(0.5);
        height: 100vh;

        img {
            height: 100%;
            object-fit: cover;
            width: 100%;
        }
    }

    .hero__title {
        font-size: 3.2rem;
        letter-spacing: -2px;
        margin-block-end: 6rem;
        margin-inline: auto;
        text-align: center;
    }

    .hero__subTitle {
        font-weight: 400;
        letter-spacing: unset;
        margin-block-end: 4.5rem;
        margin-inline: auto;
        text-align: center;
        text-transform: unset;
        width: 60%;
    }

    .hero__overlay {
        background: linear-gradient(180deg, rgba(0, 0, 0, 0) 55%, rgb(0, 0, 0) 99%);
        flex: none;
        inset: 0;
        overflow: hidden;
        position: absolute;
    }

    @media all and (min-width: 768px) {

    }

    @media all and (min-width: 992px) {
        .hero__title {
            font-size: 4.8rem;
        }
    }

    @media all and (min-width: 1440px) {
    
    }
    
</style>